<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create booking</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    .navbar {
      background-color: #1e293b;
      padding: 12px 0;
    }

    .nav-container {
      width: 90%;
      margin: auto;
      display: flex;
      align-items: center;
    }

    .logo a {
      color: #38bdf8;
      font-size: 22px;
      font-weight: bold;
      text-decoration: none;
    }

    .nav-links {
      list-style: none;
      display: flex;
      gap: 24px;
      padding-left: 30px;
    }

    .nav-links li a {
      color: #e5e7eb;
      text-decoration: none;
      font-size: 16px;
      transition: color 0.3s;
    }

    .nav-links li a:hover {
      color: #38bdf8;
    }

    .container {
      width: 90%;
      margin: 24px auto;
      display: grid;
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .card {
      border: 1px solid #e5e7eb;
      border-radius: 12px;
      padding: 16px;
      background: #fff;
    }

    .title {
      font-size: 18px;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .muted {
      color: #6b7280;
      font-size: 14px;
    }

    .error {
      background: #fee2e2;
      color: #991b1b;
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid #fecaca;
      margin-bottom: 10px;
    }

    form {
      display: grid;
      gap: 12px;
      margin-top: 10px;
    }

    label {
      font-size: 14px;
      font-weight: 600;
      color: #111827;
      margin-bottom: 4px;
      display: inline-block;
    }

    input {
      width: 100%;
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid #d1d5db;
      outline: none;
    }

    input:focus {
      border-color: #38bdf8;
      box-shadow: 0 0 0 3px rgba(56, 189, 248, 0.25);
    }

    .row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
    }

    .btn {
      background: #38bdf8;
      color: #0b1220;
      font-weight: 700;
      border: none;
      padding: 10px 14px;
      border-radius: 10px;
      cursor: pointer;
      width: fit-content;
    }

    .btn:hover {
      filter: brightness(0.95);
    }
  </style>
</head>

<body>
  <nav class="navbar">
    <div class="nav-container">
      <div class="logo">
        <a href="/">PTTP</a>
      </div>

      <ul class="nav-links">
        <li><a href="/">Trang chủ</a></li>
        <li><a href="/cars">Cars</a></li>
        <li><a href="/bookings/getAllBooking">Booking</a></li>
      </ul>
    </div>
  </nav>

  <div class="container">
    <div class="card">
      <div class="title">Rent car</div>
      <div class="muted">
        Car: <b><%= car.brand %></b> - <b><%= car.model %></b> | Price/day: <b><%= car.pricePerDay %></b>
      </div>

      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } %>

      <form method="POST" action="/bookings/createbooking/<%= car.id || car._id %>">
        <div>
          <label>User ID</label>
          <input name="userId" placeholder="Nhập userId" value="<%= (form && form.userId) ? form.userId : '' %>" required />
        </div>

        <div class="row">
          <div>
            <label>Start date</label>
            <input type="date" name="startDate" value="<%= (form && form.startDate) ? form.startDate : '' %>" required />
          </div>
          <div>
            <label>End date</label>
            <input type="date" name="endDate" value="<%= (form && form.endDate) ? form.endDate : '' %>" required />
          </div>
        </div>

        <button class="btn" type="submit">Confirm rent</button>
      </form>
    </div>
  </div>
</body>

</html>